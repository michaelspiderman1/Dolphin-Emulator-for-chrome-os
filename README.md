# Dolphin-Emulator-for- chrome-os
go to chrome-os settings  and turn On” in the Linux beta 
 
 her is the git clone https://github.com/dolphin-emu/dolphin.git
sudo apt update
sudo apt install dolphin-emu
